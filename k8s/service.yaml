apiVersion: v1
kind: Service

metadata:
  name: devops-webapp-service

spec:
  type: NodePort

  selector:
    app: devops-webapp

  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
