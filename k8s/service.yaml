apiVersion: v1
kind: Service
metadata:
  name: devops-webapp-service
spec:
  type: NodePort
  selector:
    app: devops-webapp
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30769
